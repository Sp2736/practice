# LeetCode Problem 1281: Subtract the Product and Sum of Digits of an Integer

## Problem Description
Given an integer number `n`, return the difference between the product of its digits and the sum of its digits.

## Solution Details

### Standard Approach (`problem-1281.cpp`)

- **Origin**: Human Written.
- **Algorithm**: Digit extraction via loop (`%` and `/` operations).
- **Time Complexity**: $O(\log_{10}(N))$ - Loop runs proportional to the number of digits in $N$.
- **Space Complexity**: $O(1)$ auxiliary - Only constant extra variables are used.
- **Pros & Cons**: Optimal, simple, and direct. No separate optimized version is necessary.

> **Note**: All source file documentations were generated using LLM / AI.
